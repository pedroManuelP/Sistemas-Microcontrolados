#ifndef LOOKUPTABLES_H
#define LOOKUPTABLES_H
#include <stdint.h>
#include <math.h>
//adicionar as implementação no arquivo .c depois de testadas

/*
void gen_square_wave(uint8_t *buffer, float amplitude_v, float offset_v, uint8_t duty_cycle);
void gen_rampa_wave(uint8_t *buffer, float amplitude_v, float offset_v);
void gen_triangular_wave(uint8_t *buffer, float amplitude_v, float offset_v, uint8_t duty_cycle);
void generate_sine_wave(uint8_t *buffer, float amplitude_v, float offset_v, float frequency= 30.0f, float sample_rate= 1000.0f);
*/

#endif